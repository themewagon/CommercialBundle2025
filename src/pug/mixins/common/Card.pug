mixin Card(config)
  .card&attributes(attributes)
    .card-header
      h5.mb-0(data-anchor=config.anchor)!=config.title
    .card-body.bg-body-tertiary
      block

mixin ComponentCard(config)
  - var config = Object.assign({ anchor: false,bodyClass:'bg-light' }, config);
  - var previewId = `dom-${uuid()}`;
  - var codeId = `dom-${uuid()}`;
  div&attributes(attributes)
    if config.title
      h4(class=config.titleClass && config.titleClass)=config.title
    if config.subTitle
      h5(class=config.subTitleClass && config.subTitleClass)=config.subTitle
    if config.description
      p(class=config.descriptionClass && config.descriptionClass)!=config.description
    block 
    if config.prismTitle 
      h5.mb-0(class=config.prismTitleClass && config.prismTitleClass)=config.prismTitle
    pre.scrollbar.rounded-1(class=config.prismClass ? config.prismClass : 'mt-2')
      code.language-html
        +escape
          block

mixin ComponentInvertedCard(config)
  - var config = Object.assign({ anchor: false,bodyClass:'bg-light' }, config);
  - var previewId = `dom-${uuid()}`;
  - var codeId = `dom-${uuid()}`;
  div&attributes(attributes)
    if config.title
      h4(class=config.titleClass && config.titleClass)=config.title
    if config.subTitle
      h5(class=config.subTitleClass && config.subTitleClass)=config.subTitle
    if config.description
      p(class=config.descriptionClass && config.descriptionClass)!=config.description
    if config.prismTitle 
      h5.mb-0(class=config.prismTitleClass && config.prismTitleClass)=config.prismTitle
    pre.scrollbar.rounded-1(class=config.prismClass ? config.prismClass : 'mt-2')
      code.language-html
        +escape
          block
    .py-5 
      block 

mixin ComponentInvertedGridCard(config)
  - var config = Object.assign({ anchor: false,bodyClass:'bg-light' }, config);
  - var previewId = `dom-${uuid()}`;
  - var codeId = `dom-${uuid()}`;
  div&attributes(attributes)
    if config.title
      h4(class=config.titleClass && config.titleClass)=config.title
    if config.subTitle
      h5(class=config.subTitleClass && config.subTitleClass)=config.subTitle
    if config.description
      p(class=config.descriptionClass && config.descriptionClass)!=config.description
    if config.prismTitle 
      h5.mb-0(class=config.prismTitleClass && config.prismTitleClass)=config.prismTitle
    pre.scrollbar.rounded-1(class=config.prismClass ? config.prismClass : 'mt-2')
      code.language-html
        +escape
          block
    .py-1 
      block 


mixin ComponentCardLib(config)
  - var config = Object.assign({ anchor: false,lang:'html' }, config);  
  if config.title
    h5(class=config.titleClass && config.titleClass)=config.title
  if config.description
    p(class=config.descriptionClass && config.descriptionClass)!=config.description
  pre.scrollbar.mt-2&attributes(attributes)
    code(class=`language-${config.lang}`)
      +escape
        block

mixin BootstrapComponentLink(config={})
  p For detail documentation, 
    a(href=`${config.link}` target="_blank").font-sans-serif  View #{config.name} on Bootstrap
      span.d-inline-block.ms-1 &xrarr;
